@using csharp_training_202605.Presentations.ViewModels
@model EmployeeUpdateViewModel;
@{
    ViewData["Title"] = "社員更新(完了)";
}
<h2 class="text-center mt-4">@ViewData["Title"]</h2>
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-sm-8 text-center">
            <div class="alert alert-success" role="alert">
                社員情報の更新が完了しました。
            </div>
            <table class="table table-hover">
                <tbody>
                    @*更新した社員と所属部署を表示する*@
                    <tr>
                        <td class="fw-bold">@Html.DisplayNameFor(m => m.Name)</td>
                        <td>@Html.DisplayFor(m => m.Name)</td>
                    </tr>
                    <tr>
                        <td class="fw-bold">@Html.DisplayNameFor(m => m.Email)</td>
                        <td>@Html.DisplayFor(m => m.Email)</td>
                    </tr>
                    <tr>
                        <td class="fw-bold">@Html.DisplayNameFor(m => m.Phone)</td>
                        <td>@Html.DisplayFor(m => m.Phone)</td>
                    </tr>
                    <tr>
                        <td class="fw-bold">@Html.DisplayNameFor(m => m.DeptName)</td>
                        <td>@Html.DisplayFor(m => m.DeptName)</td>
                    </tr>
                </tbody>
            </table>
            @*メニューに戻る*@
            <a asp-action="Show" asp-controller="EmployeeShow" class="btn btn-secondary mt-3 ms-2">
                社員一覧に戻る
            </a>
        </div>
    </div>
</div>